If you are running Windows, run the start.bat file. 
If you are running Linux, make the shell script executable (chmod +x start.sh), then start it (./start.sh).

Then follow the instructions the program displays. 
